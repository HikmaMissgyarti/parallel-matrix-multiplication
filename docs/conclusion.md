# Conclusion

This project successfully implemented matrix multiplication using both sequential and parallel approaches.

The experimental results showed that parallel processing reduced the execution time from 24.48 seconds to 9.60 seconds.

A speedup of approximately 2.55x was achieved, demonstrating that multiprocessing can improve performance for computationally intensive tasks.

The project also provided practical experience in applying parallel computing concepts using Python and evaluating their impact on system performance.
